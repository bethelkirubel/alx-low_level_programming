#ifndef FUNCTION_LIKE_MACRO_H
#define FUNCTION_LIKE_MACRO_H

/**
 * 3-function_like_macro.h - file that defines ABS to find the absolute value
 * @x: input
 */

#define ABS(x) ((x) < 0 ? (-x) : (x))

#endif
