#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: a program that prints "with proper grammar, but the outcome is a piece of art," and returns 0
 *
 * Return: Always 0 (Success)
 */
int main(void);
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return(0);
}
