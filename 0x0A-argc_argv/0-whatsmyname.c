#include "main.h"
#include <stdio.h>

/**
 * main -  prints the program name
 * @argc: counts argument
 * @argv: lists arguments
 *
 * Return: 0
 *
 */

int main(int argc, char *argv[])
{
(void)argc;

printf("%s", argv[0]);
return (0);
}
