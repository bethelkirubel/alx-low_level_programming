#include "main.h"
#include <stdio.h>

/**
 * is_prime_number - function that picks prime numbers
 * @n: intiger
 *
 * Return: 1 if prime and 0 if otherwise
 */

int is_prime_number(int n)

if (is_prime_number(n))
{
return (1);
}
return (0);
}
